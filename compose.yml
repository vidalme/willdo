version: '3.18'
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: vidalme/willdo-img:v4
    ports:
      - "8080:3000"
    environment:
      - NODE_ENV=production